import React from "react";
import ChangingNavBar from "../components/ChangingNavBar";

const Navbar = () => {
  return (
    <header>
      {ChangingNavBar()}
    </header>
  );
};

export default Navbar;
